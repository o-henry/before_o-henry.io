import PortfolioPost from "../../components/portfolio.post";

export const meta = {
  title: "Introduction to Next JS",
  description: "Getting started with the Next framework",
  date: "Aug 04, 2020",
  readTime: 2,
};

export default ({ children }) => (
  <PortfolioPost meta={meta}>{children}</PortfolioPost>
);

## TESTER

<pre>
  <div>TESTER</div>
</pre>
