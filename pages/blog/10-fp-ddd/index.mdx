import Post from "../../../components/post";

export const meta = {
    title: "Domain Modeling Made Functional",
    description: "함수형 프로그래밍",
    date: "2022-08-23T21:41:00.0Z",
    readTime: 30,
};

export default ({ children }) => <Post meta={meta}>{children}</Post>;

<div style={{ marginTop: "4rem", fontFamily: "Recoleta, NotoSansKR-Thin, MalgunGothic", fontSize: '11pt', lineHeight: '22pt', letterSpacing: '.8px'}}>

    FP with DDD <sup><a href="#fn1" id="fnref1"> ¹ </a></sup> 타입과 함수를 통해서 도메인 모델을 구현하는 방법을 배운다. 객체 지향적 사고라고 생각하던 DDD 에 FP를 어떻게 적용할지를 고민해보자


    > 소프트웨어 개발을 입력(요구 사항) 과 출력(최종 결과)이 있는 파이프라인 이라 생각하면 `garbage in, garbage out` 규칙이 적용된다.

    # Focus on business event
    데이터 구조보다 비즈니스 이벤트에 집중해야 한다. 비즈니스는 단순한 데이터가 아니다. 어떻게든 변화 한다.
    도메인 이벤트는 모델링 하려는 거의 모든 비즈니스 프로세스의 시작점이다. 이벤트를 도출하기 위해 이벤트 스토밍을 사용한다.

    <br />

    # Documenting Commands
    이벤트 스토밍을 통해 이벤트를 식별했다고 가정한뒤 해당 이벤트가 촉발시키는 커맨드를 식별해야 한다. 커맨드는 실패할 수 도 있지만 성공하게 되면 해당 도메인 이벤트를 생성하는 워크플로우를 시작한다.
    커맨드와 도메인 이벤트의 관계는 input-output 관계처럼 보인다.


    <br />


    <hr style={{ border:".1px solid whiteSmoke" }} />

    <div style={{ height: "15px" }}></div>

    <ol>
        <li id="fn1" style={{ fontFamily: "Poppins" }} >
            Domain Modeling Made Functional - Scott Wlaschin <a href="#fnref1"></a>
        </li>
    </ol>

</div>
