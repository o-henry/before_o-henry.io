import Post from "../../../components/post";

export const meta = {
  title: "SICP",
  description: "컴퓨터 프로그램의 구조와 해석",
  date: "2021-04-21T23:45:00.0Z",
  readTime: 10,
};

export default ({ children }) => <Post meta={meta}>{children}</Post>;

<div style={{ textAlign: "center", fontFamily: "Rajdhani" }}>
  Structure and Interpretation of Computer Programs
</div>

<div style={{  marginTop: "4rem", fontFamily: "NotoSansKR-Thin, MalgunGothic", fontSize: '11pt', lineHeight: '22pt', letterSpacing: '.8px'}}>

# Building Abstractions with Procedures

제대로 된 컴퓨터 시스템은 모듈방식(modular manner)에 따라 부품 단위로 만들고 고쳐 쓰고 갈아 끼울 수 있도록 설계되어 있다.

<hr />

프로그램을 짤 때 우리는 프로시저와 데이터라는 두가지를 쓴다. 데이터는 프로시저에서 쓰려는 물건이고, **프로시저**란 데이터를 처리하는 규칙을 적은 것이다.

<hr />

- **primitive expression** ⇢ 가장 단순한 것을 나타낸다
- **means of combination** ⇢ 간단한 것을 모아 복잡한 것으로 만든다
- **means of abstraction** ⇢ 복잡한 것에 이름을 붙여 하나로 다룰 수 있게 간추린다.

<hr />

<div className="blog-post">

```scheme
(+ 137 349) ;137 + 349 = 486
   └── 연산자(operator)
       └── 피연산자(operand)  ⇢ 엮은식(combination)
```

</div>

위와 같은 방식을 **prefix notation** 라고 한다.

<div className="blog-post">

```scheme
(define pi 3.141592)

(define radius 10)

(* pi (* radius radius)) ;314.159
```

</div>

`define` 프로시저로 변수를 저장하며, 생성한 이름으로 데이터에 접근 가능하다. 어떤 값에 이름을 붙여두었다가 뒤에 이름으로 필요한 값을 찾아 쓸 수 있다는 말은 실행기 속 어딘가에
name-object 를 저장해둔 메모리가 있다는 뜻이다. 이런 기억공간을 **environment** 라고 한다.

<br />

### Newton method

<hr />

프로시저들은 여러 매개변수에 따라 달라지는 값을 정의한다는 점에서 수학에서의 함수와 비슷하다. 하지만 이 사이에는 아주 큰 차이점이 있다. 함수와 프로시저의 차이는, 무엇이 어떤 성질을 지니는지 밝히는 일과, 그 무엇을 어떻게 만들지 또는 구할지 나타내는 일의 차이점과 같다.
이를 흔히 문제가 무엇인지에 관한 지식 **declarative knowledge** 와 문제 푸는 방법에 대한(명령) 지식 **imperative knowledge**의 다른점 이라고 한다.
수학은 (그것이 무엇인지) 선언하는 일에 무게를 두지만, 컴퓨터 과학에서는 (그것을 어떻게 만드는지 또는 구하는지) 명령하는 일에 초점을 둔다.

</div>
